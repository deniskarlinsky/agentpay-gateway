# Buyer simulator — environment template.
# Copy this file to `.env.local` and edit values for local development.

# Base URL of the AgentPay gateway. Default matches the Java buyer-client
# (`services/buyer-client/.../application.yml`). No trailing slash.
VITE_GATEWAY_BASE_URL=http://localhost:8080
